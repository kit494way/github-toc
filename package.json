{
  "name": "github-toc",
  "version": "0.1.1",
  "author": "KITAGAWA Yasutaka <kit494way@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "yarn run build:firefox && yarn run build:chrome",
    "build:firefox": "env TARGET=firefox ./scripts/build.bash firefox",
    "build:chrome": "./scripts/build.bash chrome",
    "clean": "rm -fr ./web-ext-artifacts/*"
  },
  "devDependencies": {
    "web-ext": "^8.9.0",
    "webextension-polyfill": "^0.12.0"
  }
}
